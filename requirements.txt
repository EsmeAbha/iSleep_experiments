# Pinned to the versions the reproduction was verified against.
# CPU-only torch:  pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
matplotlib>=3.7
pandas>=2.0
openpyxl>=3.1        # subject_description.xlsx (AHI / severity)
jupyter>=1.0
pytest>=7.4
